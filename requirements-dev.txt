# Development / CI dependencies.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
ruff==0.8.6
httpx==0.28.1        # also used by the FastAPI TestClient
